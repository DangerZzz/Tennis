import 'package:soft_weather_tennis/features/temp/api/client.dart';

/// Репозиторий для temp
class TempScreenRepository {
  final TempScreenClient _tempScreenClient;

  /// Конструктор [TempScreenRepository]
  TempScreenRepository(this._tempScreenClient);
}
