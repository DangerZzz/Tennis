import 'package:soft_weather_tennis/user_notifier/api/client.dart';

/// Репозиторий для сущности пользователя
class UserRepository {
  /// Конструктор [UserRepository]
  UserRepository(UserClient userClient);
}
