/// Модель, описывающая все токены для запросов на сервер
class SessionTokens {
  /// куковский токен
  final String sessionToken;

  /// Конструктор [SessionTokens]
  SessionTokens({
    required this.sessionToken,
  });
}
